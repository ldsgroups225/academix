import { createNavigationContainerRef } from '@react-navigation/native';
import { NavigationParams } from '../../utils/Routes';

export const navigationRef = createNavigationContainerRef<NavigationParams>();
